const n = 236*68-1*254165467

console.log(n)
const result = n%2 === 0? "Even": "Odd"
console.log(result)


// to store data in variable after condition we can use ternary operator.
// declare variable = condition? true : false
// if we only have one condition we can use ternary operator, but in multiple condition we have to use normal if else.